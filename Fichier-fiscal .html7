<!doctype html>
<html lang="fr">
<head>
<meta charset="utf-8">
<meta name="viewport" content="width=device-width,initial-scale=1,maximum-scale=1">
<meta name="theme-color" content="#07090a">
<title>iCHEF — Fichier fiscal complet</title>
<style>
:root{--bg:#f2f3f4;--panel:#fff;--ink:#111;--muted:#62696d;--line:#d9dddf;--gold:#c99924;--red:#c9293f;--green:#128344;--orange:#b86315;--blue:#1769aa;--dark:#07090a}
*{box-sizing:border-box}html,body{margin:0;min-height:100%;font-family:system-ui,-apple-system,"Segoe UI",Roboto,Arial,sans-serif;background:var(--bg);color:var(--ink)}button,input,select{font:inherit}button{cursor:pointer}
.hidden{display:none!important}.top{position:sticky;top:0;z-index:20;background:var(--dark);color:#fff;border-bottom:4px solid var(--gold);padding:15px 18px}.topline{display:flex;align-items:center;gap:12px;max-width:1500px;margin:auto}.brand{font-size:25px;font-weight:1000;letter-spacing:.5px}.brand span{color:#e2b84e}.sub{font-size:12px;color:#c7cccf;margin-top:3px}.top-actions{margin-left:auto;display:flex;gap:8px;flex-wrap:wrap}.top button{min-height:44px;border:1px solid #555;background:#111;color:#fff;border-radius:9px;padding:0 13px;font-weight:900}.top button.gold{border-color:#e2b84e;color:#e2b84e}
.wrap{max-width:1500px;margin:auto;padding:16px}.login{max-width:620px;margin:45px auto;background:#fff;border:1px solid var(--line);border-radius:18px;padding:25px;box-shadow:0 15px 50px rgba(0,0,0,.08)}.login h1{margin:0 0 7px;font-size:28px}.login p{color:var(--muted);line-height:1.5}.field{width:100%;height:52px;border:2px solid var(--line);border-radius:11px;padding:0 13px;background:#fff;color:#111;outline:none;margin:5px 0 11px}.field:focus{border-color:#111}.login button{width:100%;height:58px;border:0;border-radius:11px;background:#111;color:#fff;font-size:17px;font-weight:1000}.login-note{margin-top:12px;font-size:12px;color:#6a7073}.errorbox{margin-top:12px;padding:10px;border:1px solid #e4a7af;background:#fff1f3;color:#9d1c2e;border-radius:9px;font-weight:800}
.integrity{display:flex;align-items:center;gap:14px;border:2px solid var(--green);background:#eefaf3;border-radius:14px;padding:14px 16px;margin-bottom:14px}.integrity.bad{border-color:var(--red);background:#fff1f3}.integrity .big{font-size:18px;font-weight:1000}.integrity .small{font-size:11px;color:#53605a;word-break:break-all}.cards{display:grid;grid-template-columns:repeat(6,minmax(0,1fr));gap:10px;margin-bottom:14px}.card{background:#fff;border:1px solid var(--line);border-radius:13px;padding:13px;min-height:92px}.card .label{font-size:10px;color:var(--muted);font-weight:900;text-transform:uppercase;letter-spacing:.4px}.card .value{font-size:25px;font-weight:1000;margin-top:6px}.card .note{font-size:10px;color:var(--muted);margin-top:2px}.card.bad .value{color:var(--red)}.card.good .value{color:var(--green)}
.filters{background:#fff;border:1px solid var(--line);border-radius:14px;padding:12px;margin-bottom:12px;display:grid;grid-template-columns:2fr 1fr 1fr 1fr auto;gap:8px}.filters input,.filters select{height:46px;border:1px solid #bfc5c8;border-radius:9px;padding:0 10px;background:#fff}.filters button{height:46px;border:0;border-radius:9px;background:#111;color:#fff;font-weight:900;padding:0 15px}.chips{display:flex;gap:7px;flex-wrap:wrap;margin-bottom:12px}.chip{border:1px solid #bcc2c5;background:#fff;border-radius:999px;padding:8px 12px;font-size:11px;font-weight:900}.chip.active{background:#111;color:#fff;border-color:#111}
.list{display:flex;flex-direction:column;gap:7px}.row{background:#fff;border:1px solid var(--line);border-left:6px solid #7a8387;border-radius:11px;overflow:hidden}.row.sale{border-left-color:var(--green)}.row.correction,.row.cancel{border-left-color:var(--red)}.row.error{border-left-color:#a00018}.row.validation{border-left-color:var(--orange)}.row.order{border-left-color:var(--blue)}.row.audit{border-left-color:#6d4da8}.row.fiscal{border-left-color:var(--gold)}.row-main{display:grid;grid-template-columns:150px 120px 1fr 140px 150px 40px;gap:8px;align-items:center;padding:11px 12px;cursor:pointer}.time{font-size:11px;color:#4f5659}.kind{font-size:10px;font-weight:1000;text-transform:uppercase}.summary{font-size:12px;font-weight:900;min-width:0}.summary small{display:block;color:#6d7477;font-size:10px;margin-top:3px;font-weight:700;white-space:nowrap;overflow:hidden;text-overflow:ellipsis}.amount{text-align:right;font-size:14px;font-weight:1000}.ticket{text-align:right;font-size:10px;color:#52595c;word-break:break-all}.arrow{text-align:center;font-size:19px}.details{display:none;border-top:1px solid var(--line);background:#f8f9f9;padding:13px}.row.open .details{display:block}.detail-grid{display:grid;grid-template-columns:repeat(4,minmax(0,1fr));gap:8px;margin-bottom:10px}.mini{background:#fff;border:1px solid #d9dddf;border-radius:9px;padding:9px}.mini b{display:block;font-size:9px;color:#737a7d;text-transform:uppercase}.mini span{display:block;font-size:12px;font-weight:900;margin-top:3px;word-break:break-word}.section{font-size:12px;font-weight:1000;margin:13px 0 7px}.table-scroll{overflow:auto;border:1px solid #d6dadd;border-radius:10px;background:#fff}table{width:100%;border-collapse:collapse;min-width:950px}th,td{padding:8px 7px;border-bottom:1px solid #e4e7e9;font-size:10px;text-align:left;vertical-align:top}th{background:#111;color:#fff;position:sticky;top:0}td.bad{color:#b0172c;font-weight:900}.history{display:flex;flex-direction:column;gap:5px}.hist{background:#fff;border:1px solid #d8dcde;border-radius:8px;padding:8px;font-size:10px}.hist b{font-size:11px}.hist span{color:#6b7275}.raw{width:100%;max-height:460px;overflow:auto;background:#080a0b;color:#e8ebec;border-radius:9px;padding:12px;font:11px/1.45 ui-monospace,SFMono-Regular,Consolas,monospace;white-space:pre-wrap;word-break:break-word}.more{display:block;width:100%;height:50px;margin:12px 0;border:1px solid #aeb5b8;background:#fff;border-radius:10px;font-weight:900}.empty{text-align:center;padding:50px 20px;color:#777;background:#fff;border:1px dashed #bbb;border-radius:12px}.foot{padding:25px 0 50px;color:#6a7073;font-size:10px;line-height:1.5}
@media(max-width:1100px){.cards{grid-template-columns:repeat(3,1fr)}.row-main{grid-template-columns:130px 100px 1fr 110px 35px}.ticket{display:none}.filters{grid-template-columns:1fr 1fr 1fr}.filters .search{grid-column:1/-1}.detail-grid{grid-template-columns:repeat(2,1fr)}}
@media(max-width:650px){.topline{align-items:flex-start;flex-direction:column}.top-actions{margin-left:0}.cards{grid-template-columns:repeat(2,1fr)}.filters{grid-template-columns:1fr}.filters .search{grid-column:auto}.row-main{grid-template-columns:1fr 75px 28px}.time,.kind{grid-column:auto}.summary{grid-column:1/-1;grid-row:2}.amount{grid-column:2}.ticket{display:none}.detail-grid{grid-template-columns:1fr}}
@media print{.top,.filters,.chips,.more,.login{display:none!important}.wrap{max-width:none;padding:0}.row{break-inside:avoid}.row .details{display:block}.raw{display:none}.cards{grid-template-columns:repeat(6,1fr)}}
</style>
</head>
<body>
<header class="top">
  <div class="topline">
    <div><div class="brand">iCHEF <span>FICHIER FISCAL COMPLET</span></div><div class="sub">Lecture consolidée : commandes · ventes · paiements · annulations · corrections · erreurs · validations · audit</div></div>
    <div class="top-actions hidden" id="topActions">
      <button id="refreshBtn">RECHARGER</button><button id="jsonBtn">EXPORT JSON COMPLET</button><button id="csvBtn">EXPORT CSV</button><button id="printBtn">IMPRIMER</button><button class="gold" id="backBtn">RETOUR PAD</button>
    </div>
  </div>
</header>
<main class="wrap">
  <section class="login" id="loginBox">
    <h1>Accès au fichier fiscal</h1>
    <p>Pour afficher le journal complet, saisissez le <strong>PIN restaurateur</strong>. Le PIN n’est pas enregistré dans ce fichier.</p>
    <input class="field" id="tenant" placeholder="Restaurant / tenantID" autocomplete="off">
    <input class="field" id="pin" type="password" inputmode="numeric" placeholder="PIN restaurateur" autocomplete="current-password">
    <button id="loginBtn">OUVRIR LE FICHIER FISCAL</button>
    <div class="login-note">Les données sont lues depuis le serveur iCHEF. Cette page n’efface et ne modifie aucune écriture.</div>
    <div class="errorbox hidden" id="loginError"></div>
  </section>

  <section class="hidden" id="app">
    <div class="integrity" id="integrity"><div><div class="big" id="integrityTitle">CHAÎNE D’AUDIT</div><div class="small" id="integrityText"></div></div></div>
    <div class="cards" id="cards"></div>
    <div class="filters">
      <input class="search" id="search" placeholder="Rechercher table, ticket, plat, serveur, motif, operationId, erreur…">
      <select id="kindFilter"><option value="ALL">TOUS LES TYPES</option><option value="SALE">VENTES</option><option value="CORRECTION">CORRECTIONS</option><option value="CANCEL">ANNULATIONS</option><option value="ORDER">COMMANDES</option><option value="ERROR">ERREURS</option><option value="VALIDATION">VALIDATIONS</option><option value="AUDIT">AUDIT</option><option value="FISCAL">ÉVÉNEMENTS FISCAUX</option></select>
      <input id="dateFrom" type="date"><input id="dateTo" type="date"><button id="clearFilters">EFFACER FILTRES</button>
    </div>
    <div class="chips" id="chips"></div>
    <div class="list" id="list"></div>
    <button class="more hidden" id="moreBtn">AFFICHER PLUS</button>
    <div class="foot">FICHIER FISCAL iCHEF — extraction en lecture seule. Le hash et l’audit affichés sont ceux enregistrés côté serveur. Une éventuelle exigence de certification fiscale locale reste distincte de cet écran de contrôle technique.</div>
  </section>
</main>
<script>
const SERVER_URL=(location.hostname==='localhost'||location.hostname==='127.0.0.1')?'http://localhost:10000':'https://tableau-system.onrender.com';
const $=id=>document.getElementById(id);let DATA=null,EVENTS=[],VISIBLE=250,AUTH={tenantID:'',pin:'',deviceId:'fiscal_'+Math.random().toString(36).slice(2)};
const txt=v=>String(v??'');const num=v=>{const n=Number(v);return Number.isFinite(n)?n:0};const esc=v=>txt(v).replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#039;'}[c]));
function dateVal(v){const n=Date.parse(v||'');return Number.isFinite(n)?n:0}function money(v,c='CHF'){try{return new Intl.NumberFormat('fr-FR',{style:'currency',currency:c==='EUR'?'EUR':'CHF'}).format(num(v))}catch(_){return num(v).toFixed(2)+' '+c}}
function stamp(v){const d=new Date(v||0);return Number.isFinite(d.getTime())?d.toLocaleString('fr-FR'):'—'}function dl(name,type,body){const b=new Blob([body],{type});const u=URL.createObjectURL(b);const a=document.createElement('a');a.href=u;a.download=name;document.body.appendChild(a);a.click();a.remove();setTimeout(()=>URL.revokeObjectURL(u),2000)}
function rawItems(raw){if(Array.isArray(raw?.items))return raw.items;if(Array.isArray(raw?.orderSnapshot?.items))return raw.orderSnapshot.items;if(Array.isArray(raw?.order?.items))return raw.order.items;if(Array.isArray(raw?.details?.items))return raw.details.items;return []}
function histories(raw){return raw?.tableHistory||raw?.order?.tableHistory||raw?.details?.tableHistory||raw?.orderSnapshot?.tableHistory||[]}
function event(kind,type,time,summary,raw,extra={}){return {kind,type,time:time||new Date(0).toISOString(),summary,raw,table:extra.table||'',ticket:extra.ticket||'',amount:extra.amount??null,operator:extra.operator||'',terminal:extra.terminal||'',status:extra.status||'',search:txt(summary)+' '+JSON.stringify(raw)}}
function buildEvents(){const out=[];const currency=DATA?.tenant?.currency||'CHF';
 (DATA.financialHistory||[]).forEach(r=>{const t=txt(r.type).toUpperCase();const kind=t==='CORRECTION'||num(r.total??r.amount)<0?'CORRECTION':'SALE';const table=txt(r.tableId||r.table);const ticket=txt(r.ticketNumber||r.orderSnapshot?.ticketNumber);const method=txt(r.method||r.paymentMethod||r.payment?.method);out.push(event(kind,t||'VENTE',r.serverRecordedAt||r.createdAt||r.date||r.timestamp,`${kind==='SALE'?'Vente':'Correction'} · Table ${table||'—'} · ${method||'—'}`,r,{table,ticket,amount:num(r.totalTTC??r.total??r.amount),operator:r.actor?.role||r.waiter||'',terminal:r.terminalType||r.source||'',status:r.status||''}))});
 (DATA.orderSnapshots||[]).forEach(r=>out.push(event('ORDER','COMMANDE',r.closedAt||r.updatedAt||r.createdAt,`Commande · Table ${r.tableId||r.key} · ${r.itemCount} ligne(s) · ${r.status||'—'}`,r.order,{table:r.tableId||r.key,ticket:r.fiscalReceiptReference||'',amount:r.total,operator:r.order?.updatedBy||'',terminal:'SERVEUR',status:r.status||''})));
 (DATA.cancelledItems||[]).forEach(r=>out.push(event('CANCEL','ANNULATION ARTICLE',r.cancelledAt||r.updatedAt||Date.now(),`Annulation · ${r.name||r.n||'Article'} · ${r.cancelReason||r.cancellationReason||'motif non précisé'}`,r,{table:r.tableId||'',operator:r.cancelledBy||'',status:'ANNULE'})));
 (DATA.diagnostics||[]).forEach(r=>{const type=txt(r.type).toUpperCase();const kind=(txt(r.severity).toUpperCase()==='ERROR'||txt(r.severity).toUpperCase()==='CRITICAL'||type.includes('ERROR')||txt(r.status).toUpperCase()==='REFUSED')?'ERROR':'VALIDATION';out.push(event(kind,type,r.timestamp,`${r.message||type}${r.code?' · '+r.code:''}`,r,{table:r.tableId,ticket:r.ticketNumber,operator:r.actor,terminal:r.terminal,status:r.status}))});
 (DATA.audit||[]).forEach(r=>out.push(event('AUDIT',r.action,r.timestamp,`${r.action} · ${r.entityType} · ${r.entityId}`,r,{table:r.entityType==='COMMANDE'?r.entityId:'',operator:r.details?.updatedBy||r.details?.operator||'',terminal:r.details?.terminal||'',status:'SCELLE'})));
 (DATA.fiscalEvents||[]).forEach(r=>{const v=r.value||{};out.push(event('FISCAL',v.type||r.key,v.createdAt||v.timestamp,`${v.type||r.key} · ${v.tableId||''}`,v,{table:v.tableId||'',operator:v.operator||'',terminal:v.terminal||'',status:'FISCAL'}))});
 out.sort((a,b)=>dateVal(b.time)-dateVal(a.time));EVENTS=out;render();}
function card(label,value,note='',cls=''){return `<div class="card ${cls}"><div class="label">${esc(label)}</div><div class="value">${value}</div><div class="note">${esc(note)}</div></div>`}
function renderCards(){const s=DATA.summary||{},c=DATA.tenant?.currency||'CHF';$('cards').innerHTML=[card('Ventes brutes',money(s.grossSales,c),`${s.saleCount||0} ventes`,'good'),card('Corrections',money(s.corrections,c),`${s.correctionCount||0} écritures`,'bad'),card('Net',money(s.netSales,c),'ventes + corrections'),card('Commandes',s.orderSnapshotCount||0,'snapshots serveur'),card('Annulations articles',s.cancelledItemCount||0,'motifs conservés'),card('Erreurs',s.errorCount||0,`${s.validationCount||0} validations`,'bad'),card('Audit',s.auditOperationCount||0,'opérations scellées'),card('Journal permanent',s.permanentRecordCount||0,'FiscalRecord MongoDB'),card('Historique financier',s.financialRecordCount||0,'cache + journal permanent')].join('')}
const KIND_LABEL={ALL:'TOUS',SALE:'VENTES',CORRECTION:'CORRECTIONS',CANCEL:'ANNULATIONS',ORDER:'COMMANDES',ERROR:'ERREURS',VALIDATION:'VALIDATIONS',AUDIT:'AUDIT',FISCAL:'FISCAL'};
function renderChips(){const counts={ALL:EVENTS.length};EVENTS.forEach(e=>counts[e.kind]=(counts[e.kind]||0)+1);$('chips').innerHTML=Object.entries(KIND_LABEL).map(([k,l])=>`<button class="chip ${$('kindFilter').value===k?'active':''}" data-kind="${k}">${l} · ${counts[k]||0}</button>`).join('');document.querySelectorAll('[data-kind]').forEach(b=>b.onclick=()=>{$('kindFilter').value=b.dataset.kind;VISIBLE=250;render()})}
function filtered(){const q=txt($('search').value).trim().toLowerCase(),kind=$('kindFilter').value,from=$('dateFrom').value?new Date($('dateFrom').value+'T00:00:00').getTime():0,to=$('dateTo').value?new Date($('dateTo').value+'T23:59:59').getTime():Infinity;return EVENTS.filter(e=>(kind==='ALL'||e.kind===kind)&&dateVal(e.time)>=from&&dateVal(e.time)<=to&&(!q||e.search.toLowerCase().includes(q)))}
function itemTable(raw){const items=rawItems(raw);if(!items.length)return '';return `<div class="section">DÉTAIL COMPLET DES ARTICLES</div><div class="table-scroll"><table><thead><tr><th>C</th><th>Article</th><th>Qté</th><th>Prix</th><th>Destination</th><th>Cuisson</th><th>Observation</th><th>Allergie</th><th>Statut</th><th>Annulation / motif</th></tr></thead><tbody>${items.map(i=>{const seat=num(i.seat??i.guestSeat??i.seatNumber);const cancelled=i.cancelled===true;return `<tr><td>${seat?'C'+seat:'—'}</td><td>${esc(i.name||i.n||'Article')}</td><td>${esc(i.qty??i.quantity??1)}</td><td>${esc(i.price??i.p??i.unitPrice??'—')}</td><td>${esc(i.dest||i.destination||'—')}</td><td>${esc(i.cooking||i.cuisson||'—')}</td><td>${esc(i.obs||i.observation||i.note||'—')}</td><td>${esc(i.allergy||i.allergie||i.allergies||'—')}</td><td>${esc(i.sequenceStatus||i.status||i.productionStatus||'—')}</td><td class="${cancelled?'bad':''}">${cancelled?esc((i.cancelReason||i.cancellationReason||'ANNULÉ')+' · '+stamp(i.cancelledAt)):'—'}</td></tr>`}).join('')}</tbody></table></div>`}
function payments(raw){const a=raw?.payments||raw?.payment?.details||raw?.paymentDraft?.payments||[];if(!Array.isArray(a)||!a.length)return '';return `<div class="section">PAIEMENTS</div><div class="table-scroll"><table><thead><tr><th>Moyen</th><th>Montant</th><th>Validé par</th><th>Heure</th><th>Operation ID</th><th>Ticket demandé</th></tr></thead><tbody>${a.map(p=>`<tr><td>${esc(p.method)}</td><td>${esc(p.amount)}</td><td>${esc(p.confirmedBy||'—')}</td><td>${esc(stamp(p.confirmedAt||p.createdAt))}</td><td>${esc(p.paymentRequestId||'—')}</td><td>${esc(p.receiptPreference||raw?.receipt?.preference||'—')}</td></tr>`).join('')}</tbody></table></div>`}
function historyBlock(raw){const h=histories(raw);if(!Array.isArray(h)||!h.length)return '';return `<div class="section">HISTORIQUE DE LA TABLE / QUI A FAIT QUOI</div><div class="history">${h.slice().reverse().map(x=>`<div class="hist"><b>${esc(x.action||'ACTION')}</b> · ${esc(stamp(x.at||x.timestamp))}<br><span>${esc(x.by||'—')} · ${esc(x.terminal||'—')} · ${esc(x.deviceId||'')} ${x.detail?' · '+esc(x.detail):''}</span></div>`).join('')}</div>`}
function details(e){const r=e.raw||{};const hash=r.ticketHash||r.chainHash||r.currentHash||r.hash||r.fiscalHash||'';const op=r.operationId||r.paymentRequestId||r.recordId||r.id||'';return `<div class="detail-grid"><div class="mini"><b>TYPE</b><span>${esc(e.type)}</span></div><div class="mini"><b>DATE / HEURE</b><span>${esc(stamp(e.time))}</span></div><div class="mini"><b>TABLE</b><span>${esc(e.table||r.tableId||r.table||'—')}</span></div><div class="mini"><b>TICKET</b><span>${esc(e.ticket||r.ticketNumber||'—')}</span></div><div class="mini"><b>OPÉRATEUR</b><span>${esc(e.operator||r.actor?.role||r.waiter||r.authorSignature||'—')}</span></div><div class="mini"><b>TERMINAL</b><span>${esc(e.terminal||r.terminalType||r.terminal||r.source||'—')}</span></div><div class="mini"><b>OPERATION ID</b><span>${esc(op||'—')}</span></div><div class="mini"><b>HASH / PREUVE</b><span>${esc(hash||'—')}</span></div></div>${itemTable(r)}${payments(r)}${historyBlock(r)}<div class="section">DONNÉES BRUTES 100 % DISPONIBLES</div><pre class="raw">${esc(JSON.stringify(r,null,2))}</pre>`}
function row(e,i){const c=DATA.tenant?.currency||'CHF';const cls=e.kind.toLowerCase();return `<div class="row ${cls}" data-i="${i}"><div class="row-main"><div class="time">${esc(stamp(e.time))}</div><div class="kind">${esc(e.kind)}</div><div class="summary">${esc(e.summary)}<small>${esc([e.operator,e.terminal,e.status].filter(Boolean).join(' · '))}</small></div><div class="amount">${e.amount===null||e.amount===undefined?'':money(e.amount,c)}</div><div class="ticket">${esc(e.ticket||e.table||'')}</div><div class="arrow">+</div></div><div class="details">${details(e)}</div></div>`}
function render(){if(!DATA)return;renderCards();renderChips();const arr=filtered(),shown=arr.slice(0,VISIBLE);$('list').innerHTML=shown.length?shown.map((e,i)=>row(e,i)).join(''):`<div class="empty">Aucune écriture ne correspond aux filtres.</div>`;$('moreBtn').classList.toggle('hidden',arr.length<=VISIBLE);$('moreBtn').textContent=`AFFICHER PLUS · ${Math.min(250,arr.length-VISIBLE)} SUR ${arr.length}`;document.querySelectorAll('.row-main').forEach(el=>el.onclick=()=>{const r=el.parentElement;r.classList.toggle('open');el.querySelector('.arrow').textContent=r.classList.contains('open')?'−':'+'})}
async function load(){const err=$('loginError');err.classList.add('hidden');AUTH.tenantID=txt($('tenant').value).trim();AUTH.pin=txt($('pin').value).trim();if(!AUTH.tenantID||!AUTH.pin){err.textContent='Restaurant et PIN requis.';err.classList.remove('hidden');return}$('loginBtn').disabled=true;$('loginBtn').textContent='CHARGEMENT COMPLET…';try{const r=await fetch(SERVER_URL+'/api/fiscal-file/full',{method:'POST',headers:{'Content-Type':'application/json','Accept':'application/json','X-iCHEF-Tenant':AUTH.tenantID,'X-iCHEF-PIN':AUTH.pin,'X-iCHEF-Device':AUTH.deviceId},body:JSON.stringify({tenantID:AUTH.tenantID,pin:AUTH.pin,deviceId:AUTH.deviceId,terminal:'PAD_FISCAL_FILE'}),cache:'no-store'});const j=await r.json().catch(()=>({}));if(!r.ok||j.success===false)throw new Error(j.error||'Lecture impossible');DATA=j;$('loginBox').classList.add('hidden');$('app').classList.remove('hidden');$('topActions').classList.remove('hidden');const ok=j.integrity?.auditChainValid!==false;$('integrity').classList.toggle('bad',!ok);$('integrityTitle').textContent=ok?'CHAÎNE D’AUDIT SERVEUR : INTÈGRE':'ALERTE : CHAÎNE D’AUDIT À VÉRIFIER';$('integrityText').textContent=`${j.tenant?.clientName||j.tenant?.tenantID||''} · extraction ${stamp(j.tenant?.extractedAt)} · ${j.integrity?.auditCount||0} opérations · dernier hash ${j.integrity?.lastHash||'—'}${j.integrity?.brokenAt!==null&&j.integrity?.brokenAt!==undefined?' · rupture index '+j.integrity.brokenAt:''}`;buildEvents()}catch(e){err.textContent=e.message||'Erreur';err.classList.remove('hidden')}finally{$('loginBtn').disabled=false;$('loginBtn').textContent='OUVRIR LE FICHIER FISCAL'}}

async function loadSharedFiscalShare(token){
 const err=$('loginError');
 if(err)err.classList.add('hidden');
 if(!/^[a-f0-9]{48}$/i.test(txt(token).trim())){if(err){err.textContent='Lien fiscal invalide.';err.classList.remove('hidden')}return}
 if($('loginBtn')){$('loginBtn').disabled=true;$('loginBtn').textContent='OUVERTURE DU PARTAGE…'}
 try{
   const r=await fetch(SERVER_URL+'/api/fiscal-file/share/'+encodeURIComponent(token),{method:'GET',headers:{'Accept':'application/json'},cache:'no-store'});
   const j=await r.json().catch(()=>({}));
   if(!r.ok||j.success!==true||!j.dossier)throw new Error(j.error||'Partage fiscal indisponible');
   DATA=j.dossier;
   AUTH.tenantID=j.tenantID||DATA?.tenant?.tenantID||DATA?.tenantID||'';
   AUTH.pin='';
   if(!DATA.tenant||typeof DATA.tenant!=='object')DATA.tenant={};
   if(!DATA.tenant.tenantID)DATA.tenant.tenantID=AUTH.tenantID;
   DATA.sharedReadOnly=true;DATA.sharedContentHash=j.contentHash||'';DATA.sharedCreatedAt=j.createdAt||'';
   $('loginBox').classList.add('hidden');$('app').classList.remove('hidden');$('topActions').classList.remove('hidden');
   const ok=DATA.integrity?.auditChainValid!==false;$('integrity').classList.toggle('bad',!ok);
   $('integrityTitle').textContent=ok?'PARTAGE FISCAL LECTURE SEULE · CHAÎNE INTÈGRE':'PARTAGE FISCAL · CHAÎNE À VÉRIFIER';
   $('integrityText').textContent=`${DATA.tenant?.clientName||AUTH.tenantID||''} · partage ${stamp(j.createdAt)} · empreinte ${j.contentHash||DATA.integrity?.lastHash||'—'}`;
   $('backBtn').textContent='FERMER';
   $('backBtn').onclick=()=>{if(history.length>1)history.back();else window.close()};
   buildEvents();
 }catch(e){if(err){err.textContent=e.message||'Erreur';err.classList.remove('hidden')}}
 finally{if($('loginBtn')){$('loginBtn').disabled=false;$('loginBtn').textContent='OUVRIR LE FICHIER FISCAL'}}
}
$('loginBtn').onclick=load;$('pin').addEventListener('keydown',e=>{if(e.key==='Enter')load()});$('refreshBtn').onclick=load;$('search').oninput=()=>{VISIBLE=250;render()};$('kindFilter').onchange=()=>{VISIBLE=250;render()};$('dateFrom').onchange=$('dateTo').onchange=()=>{VISIBLE=250;render()};$('clearFilters').onclick=()=>{$('search').value='';$('kindFilter').value='ALL';$('dateFrom').value='';$('dateTo').value='';VISIBLE=250;render()};$('moreBtn').onclick=()=>{VISIBLE+=250;render()};$('printBtn').onclick=()=>window.print();$('backBtn').onclick=()=>{const u='iCHEF_PAD_ORDINATEUR.html?tenantID='+encodeURIComponent(AUTH.tenantID||$('tenant').value||'');window.location.href=u};
$('jsonBtn').onclick=()=>{if(!DATA)return;dl(`iCHEF_FICHIER_FISCAL_COMPLET_${AUTH.tenantID}_${new Date().toISOString().slice(0,10)}.json`,'application/json;charset=utf-8',JSON.stringify(DATA,null,2))};
$('csvBtn').onclick=()=>{const arr=filtered(),q=v=>'"'+txt(v).replace(/"/g,'""')+'"';const lines=[['Date','Type','Sous-type','Table','Ticket','Montant','Opérateur','Terminal','Statut','Résumé'].map(q).join(';')];arr.forEach(e=>lines.push([stamp(e.time),e.kind,e.type,e.table,e.ticket,e.amount??'',e.operator,e.terminal,e.status,e.summary].map(q).join(';')));dl(`iCHEF_FISCAL_${AUTH.tenantID}_${new Date().toISOString().slice(0,10)}.csv`,'text/csv;charset=utf-8','\ufeff'+lines.join('\n'))};
const qp=new URLSearchParams(location.search);const shareToken=txt(qp.get('share')).trim();if(shareToken){$('tenant').value='';$('pin').value='';loadSharedFiscalShare(shareToken);}else{$('tenant').value=qp.get('tenantID')||localStorage.getItem('ichef_tenant_id')||'';}
</script>
</body>
</html>
